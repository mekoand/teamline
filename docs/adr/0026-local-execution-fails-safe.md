---
status: proposed
---

# 本地执行默认采用可撤销和失效安全的边界

本地执行端拥有高权限，因此第一版必须提供最小权限、秘密脱敏、单一执行租约、与提交或工作树指纹绑定的检查点，以及用户可随时触发的暂停与撤销授权。控制面失联时不能取得新授权，只能在既有边界内推进到下一个安全检查点后暂停；紧急安全规则可以让旧规则集失效并暂停委托，但不能静默替换后继续执行。本地网页默认只监听回环地址并要求安装级访问凭据，具体威胁模型与恢复机制在技术规格中验证。

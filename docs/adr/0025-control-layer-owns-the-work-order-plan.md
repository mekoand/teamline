# 工作控制层拥有委托计划而模型只提供切分建议

智能切分是工作控制层的核心能力，但不要求产品自建编码 Agent。控制层拥有委托计划的数据结构、版本、边界和质量责任；现有模型或编码工具可以根据目标提出阶段与依赖建议。计划必须包含阶段目标、代码范围、检查点、验证证据、资源方案和失败恢复入口，只有经人确认的版本才能获得执行授权。改变目标、完成条件、授权边界、生效规则集或资源上限必须形成新版本并重新确认。
